#pragma once

#define ASIOICE_USE_BOOST_ASIO @__ASIOICE_USE_BOOST_ASIO@
#define ASIOICE_ENABLE_IO_URING @ASIOICE_ENABLE_IO_URING@

#if ASIOICE_ENABLE_IO_URING > 0
#if ASIOICE_USE_BOOST_ASIO > 0
    #define BOOST_ASIO_HAS_IO_URING 1
    #define BOOST_ASIO_HAS_IO_URING_AS_DEFAULT 1
    #define BOOST_ASIO_DISABLE_EPOLL 1
#else
    #define ASIO_HAS_IO_URING 1
    #define ASIO_HAS_IO_URING_AS_DEFAULT 1
    #define ASIO_DISABLE_EPOLL 1
#endif
#endif

#ifdef NDEBUG
#define ICE_DEBUG 0
#else
#define ICE_DEBUG 1
#endif

#define ICE_IN_DEBUG                                                           \
    if constexpr (!ICE_DEBUG) {                                                \
    } else
